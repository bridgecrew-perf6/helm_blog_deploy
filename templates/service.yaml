apiVersion: v1
kind: Service
metadata:
  name: {{ include "myblog_deploy.fullname" . }}
  labels:
    {{- include "myblog_deploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myblog_deploy.selectorLabels" . | nindent 4 }}
